# the object you get back from the range() function is a range object, not a list, however, the range() supports iteration.

# three parameters accepted in this order range(lower-bound, upper-bound, step-size)

for n in range(1, 11):
	print(n)